# Frontend API base URL (required in production)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional legacy variable supported for compatibility
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Optional auth health endpoint and timeout overrides
NEXT_PUBLIC_API_HEALTH_PATH=/health
NEXT_PUBLIC_AUTH_TIMEOUT_MS=10000

